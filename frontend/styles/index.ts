import {createStitches} from '@stitches/react'

export const {
  config,
  styled,
  globalCss,
  keyframes,
  getCssText,
  css,
  createTheme,
  theme
} = createStitches({
  theme: {
    colors: {
      header: '#f4f4f4',
      body: '#fff'
    },    
  }  
})

